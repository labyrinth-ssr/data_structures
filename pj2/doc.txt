换乘：只有换线路才算。
邻接矩阵
1。构建图。如何表示一个点。矩阵中的一个值，是两个点间路。由所有点构成的集合，
维护一维数组d
EXTRACT-MIN(Q) 遍历集合Q？最短值是在。其实w是二维数组。遍历节点填入index
adj【u】和u相连的点。就是w中u这一行
v[G] d[v] pi[v] 
直接给每个站编号，然后line只要get到两个节点就能。维护line 二维数组。
显然 用set即可，读入一整条line，然后index，
第二条line，字符串匹配，若相同，则标记set A，后续加入的所有点line 2，然后set A与line2点标记。
那么，对于第三条line，不得不去匹配（反正数据量也不大emm）。然后再输出一个字符串数组。
想达到的效果。
开始摆烂——————
一次读取一条line

选择的类型分析：
关联容器：set,map
关联容器中的元素是按关键字来保存和访问的。与之相对，顺序容器中的元素是按它们在容器中的位置来顺序保存和访问的。
顺序容器: vectoer

这实在是太不完美了...